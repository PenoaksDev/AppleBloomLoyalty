<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@null"
    android:gravity="right"
    android:orientation="horizontal"
    android:padding="15dip" >
 
    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/thumbnail"
        android:layout_toRightOf="@+id/thumbnail"
        android:text="One free med hot or iced latte"
        android:textColor="#c41947"
        android:typeface="sans"
        android:textSize="23sp"
        android:focusable="false"
		android:focusableInTouchMode="false"
        android:textStyle="bold"/>
 
    <TextView
        android:id="@+id/required"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/title"
        android:textColor="#000"
        android:textSize="15dip"
        android:layout_marginTop="1dip"
        android:layout_toRightOf="@+id/thumbnail"
        android:focusable="false"
		android:focusableInTouchMode="false"
        android:text="This reward requires 150 points." />

    <TextView
        android:id="@+id/button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/required"
        android:layout_centerVertical="true"
        android:background="@drawable/basic_green"
        android:textColor="#fff"
        android:textSize="25sp"
        android:textStyle="bold"
        android:text="Redeem This >" />
 
    <TextView
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:visibility="gone"
        android:id="@+id/redeemID" />
</RelativeLayout>